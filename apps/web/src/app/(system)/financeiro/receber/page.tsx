"use client";
import Link from "next/link";
import { ArrowLeft, CheckCircle2 } from "lucide-react";
import { Badge, Card } from "@bbos/ui";
import { financeDemo } from "@/lib/finance-demo-data";
const money = new Intl.NumberFormat("pt-BR", { style: "currency", currency: "BRL", maximumFractionDigits: 0 });
export default function ReceivablesPage() { return <div className="mx-auto max-w-[1500px]"><Link href="/financeiro" className="inline-flex items-center gap-2 text-xs font-bold text-forest-700"><ArrowLeft size={14}/>Financeiro</Link><h1 className="mt-5 text-3xl font-bold">Contas a receber</h1><p className="mt-2 text-sm text-stone-500">Recebíveis faturados, vencimentos e pagamentos parciais.</p><div className="mt-6 flex flex-wrap gap-2">{["Todas", "A vencer", "Vencidos", "Parcial", "Pagos"].map((item) => <button key={item} className="rounded-xl border border-stone-200 bg-white px-3 py-2 text-xs font-semibold text-stone-600">{item}</button>)}</div><Card className="mt-5 overflow-hidden p-0"><div className="hidden grid-cols-7 gap-3 border-b bg-stone-50 p-4 text-[10px] font-bold uppercase tracking-wide text-stone-500 md:grid"><span>Cliente</span><span>Documento</span><span>Emissão</span><span>Vencimento</span><span>Valor</span><span>Aberto</span><span>Status</span></div>{financeDemo.receivablesRows.map((row) => <div key={row.document} className="grid gap-2 border-b border-stone-100 p-4 text-xs md:grid-cols-7 md:items-center"><span className="font-semibold">{row.customer}</span><span className="text-stone-500">{row.document}</span><span className="text-stone-500">07/08/2026</span><span>{row.due}</span><span>{money.format(row.amount)}</span><span className="font-bold">{money.format(row.open)}</span><Badge tone={row.status === "Vencido" ? "danger" : row.status === "Parcial" ? "warning" : "success"}>{row.status}</Badge></div>)}</Card><p className="mt-4 flex items-center gap-2 text-xs text-stone-400"><CheckCircle2 size={14}/>Dados demo; recebimentos reais dependem do PostgreSQL.</p></div>; }
