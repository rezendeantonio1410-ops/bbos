"use client"; /* eslint-disable @typescript-eslint/no-explicit-any */
import * as React from "react"; import Link from "next/link"; import { Card } from "@bbos/ui"; const API=process.env.NEXT_PUBLIC_API_URL??"http://localhost:3001/api";
export default function CommercialCommissionsPage(){const [items,setItems]=React.useState<any[]>([]);React.useEffect(()=>{fetch(`${API}/commercial/commissions`).then(r=>r.ok?r.json():[]).then(setItems)},[]);return <div className="mx-auto max-w-[1200px]"><Link href="/comercial" className="text-xs font-bold text-forest-700">← Comercial</Link><h1 className="mt-5 text-3xl font-bold">Comissões</h1><div className="mt-6 space-y-2">{items.map(item=><Card key={item.id} className="flex items-center justify-between p-4 text-xs"><span>{item.salesPerson?.user?.name??"Representante"}</span><span>{Number(item.commissionAmount).toLocaleString("pt-BR",{style:"currency",currency:"BRL"})}</span><strong>{item.status}</strong></Card>)}{!items.length&&<Card className="p-10 text-center text-sm text-stone-500">Nenhuma comissão registrada.</Card>}</div></div>}
