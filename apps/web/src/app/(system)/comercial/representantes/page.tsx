"use client";
/* eslint-disable @typescript-eslint/no-explicit-any */
import * as React from "react"; import Link from "next/link"; import { Card } from "@bbos/ui";
const API = process.env.NEXT_PUBLIC_API_URL ?? "http://localhost:3001/api";
export default function RepresentativesPage(){const [people,setPeople]=React.useState<any[]>([]);React.useEffect(()=>{fetch(`${API}/commercial/sales-people`).then(r=>r.ok?r.json():[]).then(setPeople)},[]);return <div className="mx-auto max-w-[1200px]"><Link href="/comercial" className="text-xs font-bold text-forest-700">← Comercial</Link><h1 className="mt-5 text-3xl font-bold">Representantes</h1><p className="mt-2 text-sm text-stone-500">Equipe, carteiras e desempenho comercial.</p><div className="mt-6 grid gap-3 md:grid-cols-2">{people.map((person)=><Card key={person.id} className="p-5"><div className="flex items-center justify-between"><div><h2 className="font-bold">{person.user.name}</h2><p className="mt-1 text-xs text-stone-500">{person.type} · {person.region ?? "Região não definida"}</p></div><span className="text-xs font-semibold text-forest-700">{person.portfolioAssignments?.length ?? 0} clientes</span></div><div className="mt-4 grid grid-cols-3 gap-2 text-xs"><span>Meta<strong className="mt-1 block">{person.targets?.length ?? 0}</strong></span><span>Comissões<strong className="mt-1 block">{person.commissionEntries?.length ?? 0}</strong></span><span>Status<strong className="mt-1 block">{person.status}</strong></span></div></Card>)}</div></div>}
