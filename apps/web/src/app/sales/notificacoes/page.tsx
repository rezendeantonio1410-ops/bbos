"use client";
/* eslint-disable @typescript-eslint/no-explicit-any */
import * as React from "react";
import Link from "next/link";
import { ArrowLeft, Bell, Check } from "lucide-react";
const API = process.env.NEXT_PUBLIC_API_URL ?? "http://localhost:3001/api";
export default function SalesNotificationsPage() { const [items, setItems] = React.useState<any[]>([]); const load = React.useCallback(() => { fetch(`${API}/commerce/notifications`).then((r) => r.ok ? r.json() : []).then(setItems).catch(() => setItems([])); }, []); React.useEffect(() => { load(); }, [load]); async function read(id: string) { await fetch(`${API}/commerce/notifications/${id}/read`, { method: "PATCH", headers: { "content-type": "application/json" }, body: JSON.stringify({ userId: "user-jose-rezende" }) }); load(); } return <main className="min-h-screen bg-[#F7F7F5] px-4 py-5 pb-10"><div className="mx-auto max-w-md"><Link href="/sales" className="inline-flex items-center gap-1 text-xs font-bold text-forest-700 lg:hidden"><ArrowLeft size={14}/> Início</Link><div className="mt-5 flex items-center gap-3"><span className="grid size-11 place-items-center rounded-xl bg-white shadow-sm"><Bell size={19}/></span><div><h1 className="text-2xl font-bold">Notificações</h1><p className="text-sm text-stone-500">Atualizações do seu catálogo comercial</p></div></div><div className="mt-5 space-y-3">{items.length ? items.map((item) => <article key={item.id} className={`rounded-2xl bg-white p-4 shadow-sm ${item.readAt ? "opacity-70" : ""}`}><div className="flex items-start gap-3"><span className={`mt-1 size-2 shrink-0 rounded-full ${item.readAt ? "bg-stone-300" : "bg-orange-500"}`}/><div className="min-w-0 flex-1"><p className="text-[10px] font-bold uppercase tracking-widest text-forest-700">{item.type === "PRICE_CHANGED" ? "Preço atualizado" : item.type}</p><h2 className="mt-1 text-sm font-bold">{item.title}</h2><p className="mt-1 text-xs leading-5 text-stone-600">{item.message}</p><p className="mt-2 text-[10px] text-stone-400">{new Date(item.createdAt).toLocaleString("pt-BR")}</p>{!item.readAt && <button onClick={() => read(item.id)} className="mt-3 inline-flex items-center gap-1 rounded-lg border border-stone-200 px-3 py-2 text-[11px] font-bold"><Check size={13}/> Marcar como lida</button>}</div></div></article>) : <div className="rounded-2xl border border-dashed border-stone-300 bg-white p-8 text-center text-sm text-stone-500">Você está em dia. Nenhuma notificação nova.</div>}</div></div></main>; }
